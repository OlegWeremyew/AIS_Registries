import React from 'react';

import { ReturnComponentType } from '../../types';

import styled from './Footer.module.css';

export const Footer = (): ReturnComponentType => (
  <footer className={styled.footerWrapper}>Footer</footer>
);

import React from 'react';

import { ReturnComponentType } from '../../../../../../../types';

import styles from './Password.module.scss';

export const Password = (): ReturnComponentType => (
  <div className={styles.passwordWrapper}>Password</div>
);
